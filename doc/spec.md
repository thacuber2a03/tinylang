as of right now, you can only do basic expressions with:
- comparison operators (`!=`, `==`)
- arithmetic operations (`+`, `-`, `*`, `/`)
- unary operations (`-`, `!`)
- grouping (`()`)

## types

- numbers (double floating-point literals)
- booleans (true, false)
- null
